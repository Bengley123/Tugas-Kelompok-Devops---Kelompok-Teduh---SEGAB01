<?xml version="1.0" encoding="UTF-8"?>
<project name="TUBES_PBO_Kelompok_9" default="default" basedir=".">
    <description>Builds, tests, and runs the project TUBES_PBO_Kelompok 9.</description>
    
    <property name="lib.dir" value="lib"/>
    
    <!-- Set up classpath to include the JUnit and Hamcrest libraries -->
    <path id="test.classpath">
        <fileset dir="${lib.dir}">
            <include name="*.jar"/>
        </fileset>
    </path>
    
    <target name="compile">
        <javac srcdir="src" destdir="build/classes" classpathref="test.classpath"/>
    </target>
    
    <target name="test" depends="compile">
        <junit printsummary="on" haltonfailure="true" showoutput="true">
            <classpath>
                <pathelement path="build/classes"/>
                <path refid="test.classpath"/>
            </classpath>
            <formatter type="plain"/>
            <batchtest>
                <fileset dir="build/tests">
                    <include name="**/*Test.class"/>
                </fileset>
            </batchtest>
        </junit>
    </target>
    
    <target name="default" depends="test">
        <!-- This is the default target that runs when you execute 'ant' -->
    </target>
    
    <import file="nbproject/build-impl.xml"/>
</project>
